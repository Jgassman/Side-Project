# Copy this file to ".env" and fill in your key.
# This key is read ONLY by the backend server (server/index.js) and is never
# exposed to the browser. Do NOT prefix it with VITE_.
ANTHROPIC_API_KEY=

# Optional: port for the backend API server (defaults to 3001).
# PORT=3001
